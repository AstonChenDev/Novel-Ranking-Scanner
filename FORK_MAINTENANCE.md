# Fork 维护约定

此 Fork 的 `mine` 分支保留生产使用的定制改动；`upstream` 指向原作者仓库，`origin` 指向自己的 Fork。
不要在生产目录直接拉取上游最新代码，也不要使用强制同步覆盖 `mine`。

## 上游升级

1. 确保工作区修改已经提交，从 `mine` 创建独立升级分支。
2. `git fetch upstream` 后合并上游主分支，解决冲突时保留本地业务契约。
3. 运行 `python -m unittest discover -s tests -v`；在测试环境核验完整分类分页和独立入库协议。
4. 审查后合并回 `mine`，发布固定提交，保留上一版以供回滚。

定制范围：番茄分类榜 API 完整分页和校验、`ingest` 批量导出/提交，以及对应测试。
写入目标是独立采集服务，不是业务服务器。数据库、COS、任务调度及凭据不属于本 Fork。
环境文件、下载数据和令牌不得提交到公开仓库。
